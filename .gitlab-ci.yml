stages:
  - build
  - deploy

build:
  stage: build
  script:
    - mvn clean package
  artifacts:
    paths:
      - target/petclinic.war

deploy:
  stage: deploy
  script:
    - curl --upload-file target/petclinic.war http://localhost:8080/petclinic
  only:
    - master
